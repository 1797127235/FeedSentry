# FeedSentry 运维控制台

React + Vite 单页应用。开发时将 `/api`、`/health`、`/status` 代理到 `http://127.0.0.1:8000`。

```bash
npm install
npm run dev
```

生产构建：

```bash
npm run build
```

Token 存储键：`feedsentry_token`（localStorage）。
